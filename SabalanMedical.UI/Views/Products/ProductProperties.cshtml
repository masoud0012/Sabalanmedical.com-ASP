@model ProductResponse
<div class="container">
    <div class="container row">
        <div class="col-6">
            <a asp-controller="Products" asp-action="Index"><i class="bi-arrow-return-right"></i>برگشت به محصولات </a>
        </div>
    </div>
    <div class="col-6">
        @{
            if (Model.ProductNameFr != null)
            {
                <h3 class="text-center text-danger">@Model.ProductNameFr </h3>
            }
            else
            {
                <h3 class="text-center text-danger">@Model.ProductNameEn </h3>
            }
        }
    </div>
    <div>
        <a asp-controller="Products" asp-action="AddProperty" asp-route-productID="@Model.Id">
            <i style="cursor: pointer" class="bi-file-plus fs-3 text-success">جدید</i>
        </a>
    </div>
    <table id="tbl_desc" class="table-bordered m-auto table-striped">
        <thead>
            <tr>
                <th style="min-width: 200px">عنوان</th>
                <th style="min-width: 400px">مشخصات</th>
                <th>ویرایش</th>
                <th>حذف</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.productProperties!=null)
           
            @foreach (ProductPropertyResponse item in Model.productProperties)
            {
                <tr>
                    <th><div id="desc_title">@item.PropertyTitle</div></th>
                    <th><div id="desc_text">@item.PropertyDetail</div></th>
                    <th>
                        <a asp-action="EditProperty" asp-controller="Products" asp-route-propertyId="@item.Id">
                            <i class="bi-pencil-fill"> </i>
                        </a>
                    </th>
                    <th>
                        <a asp-action="DeleteProperty" asp-controller="Products" asp-route-propertyID="@item.Id">
                            <i class="bi-trash text-danger"></i>
                        </a>
                    </th>
                </tr>
            }

        </tbody>
    </table>
</div>
